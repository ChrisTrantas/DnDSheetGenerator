extends ./base.jade
block append style
    link(rel="stylesheet", type="text/css", href="/assets/style.css")
block body
  nav
    .navlink
      a(href="/logout") Log out
    .navlink
      a(href="/maker") Your Characters
  section#makeCharacter
                form#editCharacterForm(class="editCharacterForm", name="editCharacterForm", action="/editor", method="POST")
                  label(for="name") Name: 
                  input#characterName(type="text", name="name", placeholder="Character Name")
                  label(for="age") Age: 
                  input#characterAge(type="text", name="age", placeholder="Character Age")
                  label(for="profession") Class:
                  input#characterProfession(type="text", name="profession", placeholder="Character Class")
                  label(for="race") Race: 
                  input#characterRace(type="text", name="race", placeholder="Character Race")
                  input(type="hidden", name="_csrf", value="#{csrfToken}")
                  a#editCharacterSubmit(class="editCharacterSubmit", href="#") Edit Character
  section#characters
    if !characters || characters.length == 0
        div.character
            h3.emptyCharacter No characters yet
    else
        each val in characters
            div.character
                h3.characterName Name: #{val.name}
                h3.characterAge Age: #{val.age}
                h3.characterRace Race: #{val.race}
                h3.characterProfession Class: #{val.profession}
  #characterMessage
    img#speech(src="/assets/img/speech.png", alt="speech box")
    h3
      span#errorMessage
block scripts
        script(src="/assets/maker.js")